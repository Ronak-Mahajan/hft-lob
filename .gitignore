# Build outputs - reproduce with ./build.ps1 (or the g++ lines in the README)
*.exe
*.o
*.obj
/lob_bench
/lob_parallel
/spsc_stress
/lob_replay
ci.log

# Market data is never committed: results/*manifest* names the source files,
# their sizes and hashes instead.
*.NASDAQ_ITCH50
*.NASDAQ_ITCH50.gz
*.itch
*.itch.gz

# Local tooling
.venv/
